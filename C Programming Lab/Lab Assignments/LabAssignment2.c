// Write a C program - using gets() and puts() do display the input character 
#include <stdio.h>

int main() {
    char str[100];  // Declare a character array to store the input string

    // Using gets() to read the input string from the user
    gets(str);

    // Using puts() to display the entered string
    puts(str);

    return 0;
}



